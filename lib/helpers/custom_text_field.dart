

import 'package:flutter/material.dart';

class CustomTextEditor extends StatelessWidget {
  const CustomTextEditor({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
